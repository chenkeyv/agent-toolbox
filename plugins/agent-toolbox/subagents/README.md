# Subagents

Codex subagent role configuration and notes live here.

Use this folder for reusable subagent role prompts or `config.toml` snippets.
Keep runtime-specific configuration separate from the framework-neutral
Markdown profiles in `agents/`.
