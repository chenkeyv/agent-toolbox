# MCP

MCP examples and reusable configuration snippets live here.

Keep examples token-free. Prefer `.example.toml` files that show server names,
commands, URLs, and required environment variable names without storing actual
credentials.
